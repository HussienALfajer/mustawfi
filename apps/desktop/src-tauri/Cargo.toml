[package]
name = "mustawfi-desktop"
description = "Mustawfi for Windows: the Tauri 2 shell around the web client (ADR-0010, ADR-0019)."
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
mustawfi-local-db = { path = "../../../packages/local-db/native" }
tauri = { version = "2.11.6", features = [] }

[lints]
workspace = true

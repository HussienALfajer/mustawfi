{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The main window: the local database command only.",
  "windows": ["main"],
  "permissions": ["allow-local-db"]
}

# The Windows app's API origin (`pnpm --filter @mustawfi/web build:desktop`). A deployment sets
# VITE_MUSTAWFI_API_ORIGIN in the environment instead; the server lists the app's origin in
# CLIENT_ORIGINS (`http://tauri.localhost` by default).
VITE_MUSTAWFI_API_ORIGIN=http://127.0.0.1:3000
